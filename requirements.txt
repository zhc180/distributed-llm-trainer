# Core dependencies
torch>=2.0.0
torchvision
torchaudio

# Transformers and datasets
transformers>=4.35.0
datasets>=2.14.0
tokenizers>=0.14.0

# Logging and monitoring
wandb>=0.15.0
tensorboard>=2.14.0

# Utilities
pyyaml>=6.0
tqdm>=4.65.0
numpy>=1.24.0

# Optional: Flash Attention (requires CUDA)
# pip install flash-attn --no-build-isolation

# Optional: DeepSpeed for comparison
# pip install deepspeed>=0.12.0

# Development
black>=23.0.0
isort>=5.12.0
pytest>=7.4.0
